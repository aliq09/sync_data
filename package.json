{
    "name": "sync-bridge",
    "version": "0.1.1",
    "description": "Sync Bridge \u2014 Fluent outbox sync (capture, seed, drain, apply)",
    "license": "UNLICENSED",
    "imports": {
        "#now:*": "./@types/servicenow/fluent/*/index.js"
    },
    "scripts": {
        "build": "now-sdk build",
        "deploy": "now-sdk install",
        "install:pdi": "now-sdk install --auth pdi",
        "transform": "now-sdk transform",
        "types": "now-sdk dependencies",
        "clean": "now-sdk clean",
        "auth:list": "now-sdk auth --list",
        "pack": "now-sdk pack"
    },
    "devDependencies": {
        "@servicenow/sdk": "4.12.2",
        "@servicenow/glide": "27.0.5",
        "typescript": "5.5.4"
    }
}
